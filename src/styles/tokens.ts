// Mirrors the CSS custom properties in variables.css for use in TSX props
// (e.g. icon color attributes that can't read CSS variables directly)
export const colors = {
  danger: "#e53935",
} as const;
